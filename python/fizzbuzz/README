Write a program that returns the numbers from 1 to n, where n is the upper bound. But for multiples of three return “Fizz” instead of the number
and for the multiples of five return “Buzz”. For numbers which are multiples of both three and five return “FizzBuzz“.

